//Creating the Database
>create database Student;

>show database;
>use student;

//Creating user table

>CREATE TABLE users(username varchar(25) NOT NULL, password varchar(30) NOT NULL);

//Creating Address table;
>CREATE TABLE Address(Street varchar(25) NOT NULL, City varchar(30) NOT NULL,
 State char(10) NOT NULL,Country char(10) NOT NULL, Pincode varchar(16),
Phoneno long NOT NULL);

//view table;
>show tables;

//creating table that store user address in multiple columns
>CREATE TABLE userAddress AS (SELECT username, Street, City, 
  State, Country, Pincode, Phoneno FROM users, Address);

>show tables;